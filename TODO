When run without arguments, should ssh use its won name as the host
name (that way, you can make link by host name... This works with
rsh.)

Consider changing AllowHosts/DenyHosts semantic to match that used in tcp_wrappers.

Some way to disable password and passphrase queries from scripts.

Compression before encryption (optional).

Could probably safely forward INET X11 connection in every case, as we
now check the fake cookie in the client.

Some problems with NIS+?

libwrap shows remote user as root.
