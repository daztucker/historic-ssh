Julkaise RFC/Internet Draft.

SunOS has utmp problems (slot 0?).

READMEn alku on liian pitk{.

Merge rdist into the ssh distribution.
  - patch for stdin/stdout
  - -P arguments
  - configure/makefile

When sshd Address Already in Use, print error to stderr.

mcooper@usc.edu says `tty` returns 'not a tty' when run from /etc/sshrc after 'ssh $host' where the server is a sunos 4.1.3 host.
