Snabbin dokumentaatiomuutokset.

www-sivu.

Suggestion from kivinen: being able to add/remove forwardings during
the session.  The user interface could be with a special escape
character.  The protocol does not currently support this, but it would
not be too difficult to do.  XXX Do I want to do this?

write sdist.  (rdist -P, may need more patching) XXX Not important for now.
